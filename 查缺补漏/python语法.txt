
----------------------------------------tag----------------------------------------
#zip方法，将list1,list2合并为一个元祖
tuple = zip(list1,list2)

----------------------------------------tag----------------------------------------
去除list里面的值的空格：
 list = [x for x in list1 if x != ' ']（列表推导式）

----------------------------------------tag----------------------------------------
取一个列表里随机的值：
value = random.choice(listName)